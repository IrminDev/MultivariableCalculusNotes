Calcule la divergencia de cada campo e indique si, en el punto señalado, se comporta como fuente, sumidero o ninguno de los dos.
\begin{enumerate}
    \item $\mathbf{F}(x,y)=\langle x,\,y\rangle$ en $(0,0)$.
    \item $\mathbf{G}(x,y)=\langle -2x,\,-2y\rangle$ en $(1,-1)$.
    \item $\mathbf{H}(x,y)=\langle y,\,-x\rangle$ en $(2,0)$.
\end{enumerate}
