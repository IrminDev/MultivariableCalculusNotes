Calcule la derivada direccional de las siguientes funciones en el punto y dirección indicados usando la definición límite, y verifique el resultado con la fórmula del gradiente.
\begin{enumerate}
    \item $f(x, y) = 3x^2 - 2xy + y^2$ en $(1, 2)$, dirección $\mathbf{u} = \langle 1/\sqrt{2},\, 1/\sqrt{2} \rangle$.
    \item $f(x, y) = x^2 + xy$ en $(0, 1)$, dirección $\mathbf{u} = \langle 3/5,\, 4/5 \rangle$.
\end{enumerate}
