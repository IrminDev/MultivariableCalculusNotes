Calcule el gradiente de cada función y evalúelo en el punto dado.
\begin{enumerate}
    \item $f(x, y) = \ln(x^2 + y^2)$ en $(1, 2)$.
    \item $f(x, y) = e^{x}\sin(y)$ en $(0, \pi/6)$.
    \item $f(x, y) = \arctan(y/x)$ en $(1, 1)$.
\end{enumerate}
