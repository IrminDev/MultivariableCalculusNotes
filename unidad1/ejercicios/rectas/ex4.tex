Dados los puntos $P = (1, 0, 3)$ y $Q = (5, 2, -1)$:
\begin{enumerate}
    \item Parametrizar el segmento de recta $\overrightarrow{PQ}$ con $t \in [0, 1]$.
    \item Encontrar el punto medio del segmento evaluando $t = \tfrac{1}{2}$.
    \item Encontrar el punto que divide el segmento en proporción $1:3$ desde $P$.
\end{enumerate}